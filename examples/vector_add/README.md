# vector_add

This example is the first runtime target. It will load a pre-built
`vector_add.hsaco`, copy three buffers, launch the kernel, and validate the
result.

The host program currently documents the intended API usage and will start
returning useful results when the HSA backend lands.
